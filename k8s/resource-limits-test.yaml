apiVersion: v1
kind: Pod
metadata:
  name: resource-limits-test
  namespace: devops-learning
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "500m"
          memory: "256Mi"