apiVersion: v1
kind: Service
metadata:
  name: pod-network-server-nodeport
  namespace: pod-network-lab

spec:
  type: NodePort

  selector:
    app: pod-network-server

  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
    nodePort: 30080