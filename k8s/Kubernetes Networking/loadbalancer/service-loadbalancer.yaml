apiVersion: v1
kind: Service
metadata:
  name: pod-network-server-loadbalancer
  namespace: pod-network-lab

spec:
  type: LoadBalancer

  selector:
    app: pod-network-server

  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80