apiVersion: v1
kind: Pod

metadata:
  name: scheduler-failure-test
  namespace: devops-learning

spec:
  nodeSelector:
    scheduler-demo: "true"

  containers:
    - name: test
      image: busybox:1.36
      command:
        - sh
        - -c
        - |
          echo "This container should not start"
          sleep 3600