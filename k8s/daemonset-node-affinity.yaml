apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemon-node-affinity
  namespace: devops-learning

spec:
  selector:
    matchLabels:
      app: daemon-node-affinity

  template:
    metadata:
      labels:
        app: daemon-node-affinity

    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/os
                    operator: In
                    values:
                      - linux

      containers:
        - name: agent
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Node affinity agent started"
              sleep 3600